# Secret API key used by the worker
OLLAMA-HELPER-API-Key=your-secret-api-key

# Host and port where uvicorn should bind
WORKER_HOST=0.0.0.0
WORKER_PORT=18034

# Log level (optional: info, debug, warning, error, critical)
WORKER_LOG_LEVEL=info